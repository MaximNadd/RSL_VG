# RSL_VG

<br>

# ⚠️ Critical Git LFS Check: you must install Git LFS and pull the actual weights:

- git lfs install
- git lfs clone

<br>

# Docker Commands

- docker-compose build
- docker-compose up

